commands:
  foo:
    steps:
      - run:
          name: foo
          command: echo foo
executors:
  foo:
    docker:
      - image: alpine:3.13.5
workflows:
  build:
    jobs:
      - foo
  foo:
    jobs:
      - foo
jobs:
  foo:
    executor: foo
    steps:
      - foo
